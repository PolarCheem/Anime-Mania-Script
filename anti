local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Events = ReplicatedStorage:WaitForChild("Events")

local Remote = Events:FindFirstChild("X")
if Remote then
    Remote.Name = "Y" 
    print("Remote 'x' not found in ReplicatedStorage.Events")
end